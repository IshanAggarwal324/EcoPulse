# EcoPulse AI Service — copy to .env

# MongoDB (same cluster as backend; Mongoose collection: energyreadings)
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/ecopulse
# Alternative name (also supported):
# MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/ecopulse

# Optional
PORT=8000
AI_HOST=127.0.0.1
DEBUG=false
AI_CORS_ORIGINS=http://localhost:5173
INTERNAL_SERVICE_API_KEY=replace-with-shared-internal-service-key
# Required for all non-health endpoints (same value as backend INTERNAL_SERVICE_API_KEY)

# Primary artifact path (legacy/saved layout)
MODEL_DIR=models/saved
MODEL_FILENAME=lstm_model.keras
SCALER_FILENAME=scaler.save
ALLOW_MODEL_FREE_DUMMY=true

# Registry fallback (recommended with this repository layout)
ECOPULSE_MODEL_REGISTRY_DIR=models/registry
ECOPULSE_MODEL_NAME=lstm_energy_forecast
# Optional: pin a specific version folder (e.g. 20260527_130417)
# ECOPULSE_MODEL_VERSION=

# Forecasting window settings
LOOK_BACK_DAYS=30
HISTORY_DAYS=60

# Anomaly detection (Module 4.1) — Isolation Forest meter anomaly model
ECOPULSE_ANOMALY_MODEL_NAME=meter_anomaly_detector
# Optional: pin a specific anomaly version folder
# ECOPULSE_ANOMALY_MODEL_VERSION=
ANOMALY_FEATURE_WINDOW=7
# Score (0-1, higher=more anomalous) at which a reading is flagged
ANOMALY_SCORE_THRESHOLD=0.7
# z-score / jump-ratio cutoff for deterministic reason codes
ANOMALY_ZSCORE_CAP=3.0
# Expected anomaly fraction used when training the forest
ANOMALY_CONTAMINATION=0.05
# Cap on flagged readings returned per request
ANOMALY_MAX_RESULTS=500

# MongoDB pool tuning (L8 — align with backend MONGO_MAX_POOL_SIZE when scaled)
# MONGO_MAX_POOL_SIZE=50
# MONGO_MIN_POOL_SIZE=0
# MONGO_MAX_IDLE_TIME_MS=60000
# MONGO_SERVER_SELECTION_TIMEOUT_MS=10000

# Forecast response cache (M9)
# FORECAST_CACHE_TTL_SECONDS=120
